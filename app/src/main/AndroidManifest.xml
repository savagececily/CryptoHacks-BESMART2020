<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cryptohacks.overhead">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PhotoActivity"></activity>
        <activity android:name=".EmployeeLoginActivity" />
        <activity android:name=".CarryOnAvailableActivity" />
        <activity android:name=".NoCarryOnActivity" />
        <activity android:name=".CarryOnActivityPt3" />
        <activity android:name=".CarryOnActivityPt2" />
        <activity android:name=".CarryOnActivityPt1" />
        <activity android:name=".CustomerLoginActivity" />
        <activity android:name=".UserTypeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>